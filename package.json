{
  "name": "voice-receptionist-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Bilingual EN/VI AI voice receptionist for Vietnamese nail salons and restaurants.",
  "workspaces": ["web"],
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "start": "cd web && npm start",
    "gateway:start": "cd voice-gateway && npm start",
    "gateway:check": "cd voice-gateway && npm run check",
    "lint": "cd web && npm run lint"
  },
  "engines": {
    "node": ">=18"
  }
}
